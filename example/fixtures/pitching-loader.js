exports.pitch = function(remainingRequest, previousRequest, data) {
	console.log(`debug::pitch-loader remainingRequest = ${remainingRequest}, previousRequest = ${previousRequest}, data = ${data}`);
	/*return [
		remainingRequest,
		previousRequest
	].join(":");*/
};
